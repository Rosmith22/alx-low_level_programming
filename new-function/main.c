#include <stdio.h>
/*include your hader file below this*/

/**
 * function with no return and no parameter eg. void print_hi()
 * function with return value eg. int random()
 * function with return and parameter int random(int how_many_numbers)
 * types of loop
 * for loop
 * while loop
 * do while
 */
int main() {
    int value;
    printf("Enter how many numbers you need random of: ");
    scanf("%d",&value);
 random(value,1);

    return 0;
} 
