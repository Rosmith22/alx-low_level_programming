#include "main.h"
/**
* _abs - computes the absolute value of an integer.
* @n: - character to test
* Return: converted value
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (-1 * n);
	}
	else
	{
		return (n);
	}
}
